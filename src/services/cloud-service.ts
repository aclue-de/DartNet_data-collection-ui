import axios from "axios";
import Throw from "../models/throw";

export class CloudService {

  constructor() {
  }

  /**
   * newImage
   */
  public async uploadNewThrow(t: Throw[]) {
      
  }
}
